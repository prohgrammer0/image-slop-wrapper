{
  "tasks": {
    "dev": "deno run --watch --allow-read=. --allow-write=outputs,reference-images,context,requests.yaml --allow-net main.ts",
    "start": "deno run --allow-read=. --allow-write=outputs,reference-images,context,requests.yaml --allow-net main.ts",
    "check": "deno fmt --check && deno lint && deno test --allow-read=."
  },
  "fmt": {
    "exclude": ["context/"]
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}
